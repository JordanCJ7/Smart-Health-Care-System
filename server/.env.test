# ========================================
# TEST ENVIRONMENT CONFIGURATION
# ========================================
# This file is used ONLY when running tests
# It prevents tests from deleting your real data
# ========================================

PORT=5001
NODE_ENV=test

# ⚠️ CRITICAL: Separate test database
# Notice the "/healthcare-test" at the end - this creates a separate database
# Your main database remains untouched during tests
MONGO_URI=mongodb+srv://cj:777@cluster0.kwekzkw.mongodb.net/healthcare-test?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (same as development)
JWT_SECRET=cj7
JWT_EXPIRE=7d

# CORS Configuration
CLIENT_URL=http://localhost:5173

# PayPal Sandbox Configuration
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
