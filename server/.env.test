# Test Environment Configuration
PORT=5001
NODE_ENV=test

# IMPORTANT: Use a SEPARATE test database to avoid deleting production data
# Option 1: Add "/test" or "/testing" to your database name
MONGO_URI=mongodb+srv://cj:777@cluster0.kwekzkw.mongodb.net/healthcare-test?retryWrites=true&w=majority&appName=Cluster0

# Option 2: Use local MongoDB for testing (recommended)
# MONGO_URI=mongodb://localhost:27017/healthcare-test

# JWT Configuration (same as production)
JWT_SECRET=cj7
JWT_EXPIRE=7d

# CORS Configuration
CLIENT_URL=http://localhost:5173

# PayPal Sandbox Configuration
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-client-secret
